admin-credentials.txt

# Dependencies
node_modules/
.pnp
.pnp.js

# Production builds
dist/
build/
.next/
out/

# Testing
coverage/
.nyc_output/
*.lcov

# Environment files
.env
# Backups written by scripts/generate-secrets.sh — these contain the OLD
# secrets, so they must never be committed.
.env.bak.*
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.opencode/*
.planning/*


# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Cache
.cache/
.parcel-cache/
.eslintcache/
.stylelintcache/
*.tsbuildinfo

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Lock files (keep package-lock.json, ignore others)
yarn.lock
pnpm-lock.yaml

# Misc
*.bak
*.swp
*~

# Docker
.docker/

# MongoDB data (if running locally)
data/db/

# Jest
jest-results.json

# Uploads (if using local storage)
uploads/

# Generated during npm install
public/wasm/
public/models/blaze_face_short_range.tflite

# Mobile (mobile/ — Expo/React Native). mobile/.gitignore covers this
# directory's own build/cache noise (.expo/, coverage/, etc.); these are
# repo-wide safety nets so signing material and build artifacts can never
# land in a commit even if something outside mobile/ ever produces one.
*.jks
*.keystore
*.mobileprovision
*.apk
*.aab
*.ipa
/mobile/android
/mobile/ios

# Rust / Cargo
/target/
**/target/
**/*.rs.bk
*.rs.bk
cargo-timing.html
.cargo/config.toml.local

#Agent files
CLAUDE.md
AGENTS.md
DESIGN.md
.claude/
